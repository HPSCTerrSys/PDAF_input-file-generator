[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "pdaf-input-file-generator"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "gitpython",
]

[tool.setuptools]
packages = []
